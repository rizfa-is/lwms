# Session notes

This file is loaded by opencode at session start (see `opencode.json`).
Use it as rolling memory for in-progress work that should survive a
restart. Keep it short — long-term decisions belong in `CLAUDE.md` or
`docs/`.

## Status

Scaffold initialised. No strategy work yet.

## Last actions

(empty)
